{
  "name": "smart-parking-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev":     "vite",
    "build":   "vite build",
    "preview": "vite preview",
    "deploy":  "npm run build && firebase deploy --only hosting"
  },
  "dependencies": {
    "chart.js": "^4.4.0",
    "firebase": "^12.13.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
